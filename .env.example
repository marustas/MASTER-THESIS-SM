# Copy this file to .env and fill in your values
# Never commit .env to git

# Job platform credentials (Step 2)
LINKEDIN_EMAIL=
LINKEDIN_PASSWORD=
UPWORK_API_KEY=

# Scraping settings
REQUEST_DELAY_SECONDS=2.0
MAX_RETRIES=3
USER_AGENT=Mozilla/5.0 (compatible; thesis-research-bot/1.0)

# Data paths (override defaults if needed)
# DATA_DIR=data

# MLflow experiment tracking (Step 8-11)
MLFLOW_TRACKING_URI=./experiments/mlruns
